# Active Context

## Current Status
- `main` is synced and branch protection is re-enabled.
- Current working branch is `pyjenkinsapi-v0.1.2`.
- `rigor-cli` is vendored under `tools/rigor-cli/` and treated as read-only tooling.
- Repo-local `ai-*` helpers, the legacy `pyjenkinsapi-review` alias, and GitHub Actions CI are in place.
- Azure helper scripts and plans remain queued for follow-up work.

## Current Focus
- Keep the next branch narrow and scoped.
- Preserve the vendored tooling boundary and the compatibility alias.
- Next planned task remains the Azure PR branch-policy helper.

## Notes
- Update this file after the next milestone or direction change.
